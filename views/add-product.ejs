<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <title>Tambah Produk</title>
  <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-gray-100 text-gray-800 font-sans">
  <div class="max-w-4xl mx-auto p-6 mt-10 bg-white rounded-xl shadow-md">
    <h2 class="text-2xl font-bold mb-6">Tambah Produk Baru</h2>
    <form method="POST" action="/add-product" class="grid grid-cols-2 gap-4">
      
      <div>
        <label class="block mb-1 font-medium">Reference *</label>
        <input type="text" name="reference" required placeholder="PRD001" class="input-field">
      </div>

      <div>
        <label class="block mb-1 font-medium">Brand *</label>
        <input type="text" name="brand" required placeholder="Nama Brand" class="input-field">
      </div>

      <div>
        <label class="block mb-1 font-medium">Nama Produk *</label>
        <input type="text" name="product_name" required placeholder="Nama Produk" class="input-field">
      </div>

      <div>
        <label class="block mb-1 font-medium">Variant *</label>
        <input type="text" name="variant" required placeholder="Ukuran / Warna" class="input-field">
      </div>

      <div>
        <label class="block mb-1 font-medium">Kategori *</label>
        <input type="text" name="category" required placeholder="Kategori Produk" class="input-field">
      </div>

      <div>
        <label class="block mb-1 font-medium">Harga (Rp) *</label>
        <input type="number" name="price" required placeholder="10000" class="input-field">
      </div>

      <div>
        <label class="block mb-1 font-medium">Diskon (%)</label>
        <input type="number" name="discount_percentage" value="0" min="0" max="100" class="input-field">
        <small class="text-xs text-gray-500">Kosongkan jika tidak ada diskon</small>
      </div>

      <div>
        <label class="block mb-1 font-medium">Stok *</label>
        <input type="number" name="stock" required placeholder="10" class="input-field">
      </div>

      <div class="col-span-2">
        <label class="block mb-1 font-medium">EAN Number *</label>
        <input type="text" name="ean_number" value="0000000000000" class="input-field">
        <small class="text-xs text-gray-500">Boleh dummy: 13 digit angka</small>
      </div>

      <div class="col-span-2">
        <label class="block mb-1 font-medium">URL *</label>
        <input type="url" name="url" value="https://example.com" class="input-field">
        <small class="text-xs text-gray-500">Harus valid. Contoh: https://tokomu.com/produk/123</small>
      </div>

      <div class="col-span-2">
        <label class="block mb-1 font-medium">Deskripsi</label>
        <textarea name="description" rows="3" class="input-field" placeholder="Deskripsi singkat produk"></textarea>
      </div>

      <div class="col-span-2">
        <label class="block mb-1 font-medium">Gambar (pisahkan dengan koma)</label>
        <input type="text" name="images" placeholder="https://img.com/1.jpg, https://img.com/2.jpg" class="input-field">
        <small class="text-xs text-gray-500">Boleh dikosongkan. Format: link gambar, dipisah koma</small>
      </div>

      <div class="col-span-2 flex justify-between items-center mt-4">
        <a href="/dashboard" class="text-blue-500 hover:underline">← Kembali ke Dashboard</a>
        <button type="submit" class="bg-blue-600 text-white px-6 py-2 rounded hover:bg-blue-700 transition">Simpan Produk</button>
      </div>
    </form>
  </div>

  <style>
    .input-field {
      width: 100%;
      padding: 0.5rem;
      border: 1px solid #cbd5e1;
      border-radius: 0.375rem;
    }
    .input-field:focus {
      outline: none;
      border-color: #3b82f6;
      box-shadow: 0 0 0 1px #3b82f6;
    }
  </style>
</body>
</html>
